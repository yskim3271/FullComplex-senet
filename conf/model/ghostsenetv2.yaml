model_lib: ghostsenetv2
model_class: GhostSEnet
input_type: am

param:
  win_len: 400
  hop_len: 100
  fft_len: 400
  compress_factor: 0.3
  dense_channel: 64
  sigmoid_beta: 2.0
  num_tsblock: 4
  dense_mode: attn
  mask_mode: attn
  phase_mode: attn

